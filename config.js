export const timing = {
    batchTaskDelay: 100,
    newBatchDelay: 400
};

export const verbosity = 3;

export const schedulerConfig = {
    maxBatches: 250,
    statsInterval: 10000, //ms
    updateInterval: 50, //ms
    hackAmount: 0.10,
    maxRunners: 1
}