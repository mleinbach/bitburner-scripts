export const timing = {
    batchTaskDelay: 100,
    newBatchDelay: 300
};

export const verbosity = 3;

export const schedulerConfig = {
    maxBatches: 200,
    statsInterval: 5000, //ms
    updateInterval: 50, //ms
    hackAmount: 0.10,
    maxRunners: 1
}